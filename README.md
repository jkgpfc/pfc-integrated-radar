# PFC Radar One

Live dashboard: https://jkgpfc.github.io/pfc-integrated-radar/

`index.html` — dashboard (reads `data.json` same-origin on every load)
`data.json`  — classified registers, overwritten by the Google Sheet after each run

Publishing is done by the Apps Script:
Sheet menu > Classification & tools > Set up GitHub publishing (repo + token).
